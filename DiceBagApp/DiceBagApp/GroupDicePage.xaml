<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DiceBagApp.GroupDicePage">
    <StackLayout>
        <StackLayout Orientation="Horizontal"  VerticalOptions="CenterAndExpand" HorizontalOptions="Center" >
            <Entry Text="1"  Keyboard="Numeric" WidthRequest="100"  BindingContext="{Binding Quantity}"/>
            <Label Text="D"/>
            <Entry Text="1"  Keyboard="Numeric"  WidthRequest="100"  BindingContext="{Binding Dice}" />
        </StackLayout>
        <StackLayout Orientation="Horizontal" VerticalOptions="CenterAndExpand" HorizontalOptions="Center">
            <Button Text="Add" Command="{Binding AddDiceCommand}" />
            <Button Text="Cancel" Command="{Binding CancelDiceCommand}"/>
        </StackLayout>
    </StackLayout>
</ContentPage>